// === Style

// === Services

// === Imgs

// === React

// === Child Components

// ====== Component ======
// =======================

export function ToyFilter({ /* prop1, prop2 */ }) {
    // === Consts

    // === Effects

    // === Functions

    // if (!data) return <div>Loading...</div>
    return (
        <section className="ToyFilter">
            <h1>ToyFilter</h1>
        </section>
    )
}