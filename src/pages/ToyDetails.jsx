// === Style

// === Services

// === Imgs

// === React

// === Child Components

// ====== Component ======
// =======================

export function ToyDetails({ /* prop1, prop2 */ }) {
    // === Consts

    // === Effects

    // === Functions

    // if (!data) return <div>Loading...</div>
    return (
        <section className="ToyDetails">
            <h1>ToyDetails</h1>
        </section>
    )
}